//
// Created by Samik Biswas on 30-10-2019.
//

#include "rotate.h"

void rotate(vector<int>& nums, int k) {

    int size = nums.size();
    vector<int> buffer (size, 0);

    for (int i = size - 1; i >= 0 ; --i) {

        if ()
    }

}